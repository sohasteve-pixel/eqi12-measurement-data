EQi12 Docker 网站栈性能测试
日期：2026-07-13
容器：nginx:alpine、postgres:17-alpine、redis:8-alpine

nginx本机HTTP静态页（修正后的异步测试器，15秒）：
- 10并发：6155.81 RPS，错误0，平均1.619 ms，P95 2.908 ms
- 50并发：10374.33 RPS，错误0，平均4.816 ms，P95 9.078 ms
- 100并发：10629.74 RPS，错误0，平均9.403 ms，P95 17.224 ms

PostgreSQL 17 pgbench，临时scale=10数据库：
- 10客户端/4线程/30秒：3323.39 TPS，平均延迟3.002 ms，失败事务0
- 30客户端/6线程/30秒：4384.85 TPS，平均延迟6.830 ms，失败事务0

Redis 8，独立DB 15：
- 50并发：SET 145985.41 RPS，GET 158604.28 RPS，P50约0.15–0.16 ms
- 100并发：SET 162337.66 RPS，GET 167084.38 RPS，P50约0.30 ms

nginx + PostgreSQL + Redis混合负载：
- nginx 50并发共成功468828次，错误0
- nginx平均7805.10 RPS，范围6240.67–10420.37 RPS，平均P95 14.313 ms
- PostgreSQL 20客户端：2082.95 TPS，平均延迟9.582 ms，失败事务0
- 主机CPU采样平均63.42%
- 三个容器测试后均保持healthy，无重启或服务中断

清理确认：
- PostgreSQL临时数据库eqi12_bench已删除
- Redis DB 15测试后DBSIZE=0

结论：
该机在本次本机回环、静态nginx页面和临时测试数据条件下，可同时承担nginx、PostgreSQL和Redis负载；混合负载下所有HTTP请求及数据库事务均无错误。结果不能直接等同于公网动态网站并发用户数，真实网站还会受应用代码、TLS、磁盘、网络和数据库查询复杂度影响。

数据有效性说明：
第一版同步HTTP测试器在50/100并发时发生测试端线程池饥饿，其HTTP数据已标记无效。本文只采用FIXED文件中的异步测试结果。PostgreSQL和Redis第一轮独立结果不受该问题影响。
